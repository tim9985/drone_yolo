# 학습 모델 정리

자율 정찰 드론 관제 시스템 — 비전 파트
최종 갱신 2026-08-18

지금까지 학습한 모델 전체를 한눈에 본다.
**모든 수치는 학습에 쓰지 않은 검증 데이터에서 측정한 실측값**이다.

기록 원본
- 학습 곡선 · 설정 — `runs_person/<실행이름>/results.csv`, `args.yaml`
- 도메인별 평가 — `metrics/eval_domain.csv`
- NOMAD 검증 평가 — `metrics/eval_person.csv`
- 임계값별 성능 — `metrics/threshold_*.csv`

---

## 0. 한눈에 보기

| 모델 | 클래스 | 학습 데이터 | 여름 mAP50 | 겨울 mAP50 | 쓰러짐 재현율 | 상태 |
| --- | ---: | --- | ---: | ---: | ---: | --- |
| 기성 (VisDrone) | 11 | — (사전학습만) | 0.133 | 0.544 | 0.048 | 기준선 |
| 배우 10명 | 1 | NOMAD 2,657 | 0.461 | — | 0.258 | 보관 |
| 배우 20명 | 1 | NOMAD 4,897 | 0.635 | — | 0.569 | 보관 |
| 배우 30명 | 1 | NOMAD 7,624 | **0.644** | 0.242 | 0.617 | 보관 |
| **통합** | 1 | NOMAD + WiSARD 14,342 | 0.641 | **0.922** | 0.531 | **운용 중** |
| 자세 2클래스 | 2 | NOMAD 7,624 (자세 라벨) | 진행 중 | — | — | 학습 중 |
| 합성 혼합 | 1 | 통합 + 합성 411×3 | — | — | — | 중단됨 |

> 쓰러짐 재현율은 모델마다 측정 임계값이 다르다. 같은 잣대 비교는 4장을 볼 것.

---

## 1. 기성 모델 — YOLOv8s-VisDrone

| 항목 | 값 |
| --- | --- |
| 파일 | `weights/yolov8s_visdrone.pt` (21.5 MB) |
| 클래스 | 11개 — pedestrian, people, bicycle, car, van, truck 등 |
| 학습 | 우리가 학습하지 않음. 공개 사전학습 가중치 |
| 역할 | **기준선** — "기성 기술로는 안 된다"의 근거 |

**성능 (NOMAD 검증 1,198장)**

| 지표 | 값 |
| --- | ---: |
| mAP50 | 0.133 |
| 재현율 | 0.123 |
| **쓰러진 사람 재현율** | **0.048** |
| 보행 재현율 | 0.504 |

**해석** — 걸어다니는 사람은 절반쯤 찾지만 **쓰러진 사람은 20명 중 1명**만 찾는다.
지상 시점 데이터로 학습된 모델이라 위에서 내려다본 누운 자세를 사람으로 보지 않는다.
그대로 쓰면 프로젝트가 성립하지 않는다는 것을 보여준 실측이다.

---

## 2. NOMAD 단계별 — 데이터를 늘리면 얼마나 좋아지나

같은 검증셋(배우 004·008·014·018, 1,198장 — **어느 모델의 학습에도 쓰지 않음**)에서
배우 수만 늘려가며 측정했다.

### 2-1. 배우 10명

| 항목 | 값 |
| --- | --- |
| 파일 | `weights/yolov8s_stage1_nomad.pt` |
| 실행 | `runs_person/stage1_nomad` — 35 epoch, best ep20 |
| 학습 데이터 | NOMAD 배우 1~10, 2,657장 |
| 설정 | imgsz 960 · **batch 1** · lr0 0.01 · patience 15 |

| 지표 | 값 |
| --- | ---: |
| mAP50 | 0.461 |
| 재현율 | 0.413 |
| 쓰러짐 재현율 | 0.258 |

> batch 1 로 돌린 유일한 실행이다. 자동 배치 산정이 실패해 1로 떨어졌고,
> 학습이 크게 출렁였다(ep2 0.551 → ep3 0.310). 이후 실행은 batch 8 로 고정했다.

### 2-2. 배우 20명

| 항목 | 값 |
| --- | --- |
| 파일 | `weights/yolov8s_stage1_nomad20.pt` |
| 실행 | `runs_person/stage1_nomad20` — 23 epoch, best ep12 |
| 학습 데이터 | NOMAD 배우 1~20, 4,897장 |

| 지표 | 값 | 10명 대비 |
| --- | ---: | ---: |
| mAP50 | 0.635 | +38 % |
| 재현율 | 0.553 | **+34 %** |
| 쓰러짐 재현율 | 0.569 | **+120 %** |

> **예상이 빗나간 지점.** 데이터를 늘려도 한계효용이 빠르게 줄 것이라 예상했으나
> 실측은 반대였다. 이 시점에서 데이터가 여전히 병목이라는 것이 확인되었다.

### 2-3. 배우 30명

| 항목 | 값 |
| --- | --- |
| 파일 | `weights/yolov8s_stage1_nomad30.pt` |
| 실행 | `runs_person/stage1_nomad30` — 31 epoch, best ep25 |
| 학습 데이터 | NOMAD 배우 1~30, 7,624장 |

| 지표 | 값 | 20명 대비 |
| --- | ---: | ---: |
| mAP50 | 0.643 | **+1.1 %** |
| 재현율 | 0.589 | +6.5 % |
| 쓰러짐 재현율 | 0.617 | +8.4 % |

> **여기서 포화되었다.** 20 → 30 의 상승폭이 10 → 20 의 1/30 수준이다.
> **같은 종류의 데이터를 더 모으는 것은 의미가 없다**는 결론이 나왔고,
> 이것이 계절·지형이라는 다른 축(WiSARD)으로 전환한 근거다.

### 데이터 규모 대비 재현율

| 학습 배우 | 학습 장수 | 재현율 | 쓰러짐 재현율 |
| --- | ---: | ---: | ---: |
| 0 (기성) | — | 0.123 | 0.048 |
| 10명 | 2,657 | 0.413 | 0.258 |
| 20명 | 4,897 | 0.553 | 0.569 |
| 30명 | 7,624 | 0.589 | 0.617 |

---

## 3. 통합 모델 — 현재 운용 모델

| 항목 | 값 |
| --- | --- |
| 파일 | **`weights/yolov8s_stage1_all.pt`** (64.0 MB) |
| 실행 | `runs_person/stage1_all` — 34 epoch, **best ep30** |
| 학습 데이터 | NOMAD 7,624 + WiSARD 6,718 = **14,342장** |
| 검증 데이터 | NOMAD 1,198 + WiSARD 1,571 = **2,769장** |
| 설정 | imgsz 960 · batch 8 · lr0 0.01 · patience 15 |
| 아키텍처 | YOLOv8s, 파라미터 11.14 M, 클래스 1개 (`person`) |
| 운용 임계값 | **0.15** |

**통합 검증셋 전체 성능 (2,769장)**

| 지표 | 값 |
| --- | ---: |
| mAP50 | **0.850** |
| mAP50-95 | 0.605 |
| 정밀도 | 0.866 |
| 재현율 | 0.765 |

### 도메인별 분리 평가 — 반드시 나눠서 봐야 한다

통합 점수 하나로는 아무것도 알 수 없다. 환경별로 나눈 결과다.

| 검증 환경 | 장수 | 기성 | NOMAD 전용 | **통합** |
| --- | ---: | ---: | ---: | ---: |
| 여름 · 농장 (mAP50) | 1,198 | 0.133 | **0.644** | 0.641 |
| **겨울 · 설경 (mAP50)** | 1,282 | 0.544 | 0.242 | **0.922** |

**두 가지가 확인되었다.**

1. **겨울 데이터를 넣어도 여름 성능은 안 깎였다** — 0.644 → 0.641, 차이 0.4 %.
   재현율은 0.589 → 0.606 으로 오히려 올랐다.

2. **여름 데이터만으로 학습하면 겨울에서 기성 모델보다 나빠진다** — 0.242 vs 0.544.
   "초록 배경 + 여름 옷"에 특화되면서 눈 덮인 산지를 못 읽게 된다.
   **계절 데이터 확보가 선택이 아니라 필수**라는 실측 근거다.

### 자세별 재현율 (NOMAD 검증, 임계값 0.25 기준)

| 자세 | 기성 | NOMAD 전용 | 통합 |
| --- | ---: | ---: | ---: |
| 보행 | 0.504 | 0.889 | 0.870 |
| 은폐 | 0.096 | 0.556 | 0.548 |
| **쓰러짐** | 0.048 | **0.617** | 0.531 |

> 통합이 쓰러짐에서 8.6p 낮아 보이나, **검증 표본이 209개**라 신뢰구간이 ±6.6p 다.
> 두 값의 구간이 겹치므로 통계적으로 구분되지 않는다. 4장의 임계값 조정을 볼 것.

---

## 4. 임계값 조정 — 재학습 없이 성능을 되찾다

쓰러진 사람은 신뢰도가 낮게 나온다. 기본 임계값 0.25 가 그 탐지를 버리고 있었다.

**통합 모델, 임계값별 (원본: `metrics/threshold_tuning.csv`)**

| 임계값 | 겨울 재현율 | 여름 재현율 | 쓰러짐 재현율 | 배경 오탐/장 |
| ---: | ---: | ---: | ---: | ---: |
| 0.05 | 0.915 | 0.706 | **0.675** | 1.44 |
| 0.10 | 0.905 | 0.684 | 0.641 | 1.17 |
| **0.15 (운용)** | **0.896** | **0.663** | **0.608** | **0.99** |
| 0.25 (기본) | 0.882 | 0.618 | 0.531 | 0.72 |
| 0.40 | 0.861 | 0.572 | 0.474 | 0.53 |
| 0.80 | 0.731 | 0.305 | 0.220 | 0.04 |

임계값 0.05 에서 쓰러짐 재현율이 **0.675** 로, NOMAD 전용 모델의 최고치(0.617)를 넘는다.
통합 학습이 쓰러진 자세를 못 배운 것이 아니라 기본 임계값이 낮은 확신의 탐지를 버렸던 것이다.

**같은 임계값(0.15)에서 세 모델 비교**

| 모델 | 여름 재현율 | 여름 정밀도 | 겨울 재현율 | 쓰러짐 재현율 | 배경 오탐/장 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 기성 | 0.203 | 0.250 | 0.528 | 0.053 | 1.84 |
| NOMAD 전용 | **0.668** | 0.771 | 0.234 | **0.636** | **0.26** |
| **통합** | 0.663 | **0.802** | **0.896** | 0.608 | 0.99 |

**운용값 0.15 를 고른 이유** — 재난 탐색에서 놓친 조난자와 헛탐지의 무게는 다르다.
재현율에 2배 가중을 준 F2 점수가 0.10~0.15 구간에서 최대다.
추가로 추적기가 순간 오탐을 3프레임 안에 폐기하므로 표의 오탐 수치는 최종 신고 건수보다 크다.

---

## 5. 자세 2클래스 — 진행 중

| 항목 | 값 |
| --- | --- |
| 실행 | `runs_person/pose2` |
| 시작 가중치 | `weights/yolov8s_stage1_all.pt` (통합 모델) |
| 학습 데이터 | NOMAD 7,624장 (활동 라벨로 자세 부여) |
| 클래스 | 0 `person` 정상 · 1 `fallen` 쓰러짐 |
| 설정 | imgsz 960 · batch 6 · lr0 0.01 · epochs 20 · patience 8 |

**클래스 구성**

| | 학습 | 검증 |
| --- | ---: | ---: |
| 정상 (Walking + 서 있는 Hiding) | 5,366 | 989 |
| 쓰러짐 (Hiding Laying + Laying) | 2,258 | 209 |
| 불균형 | 2.4 : 1 | 4.7 : 1 |

**현재 진행 (9 epoch 시점)**

| ep | mAP50 | mAP50-95 | P | R |
| ---: | ---: | ---: | ---: | ---: |
| 1 | 0.316 | 0.192 | 0.399 | 0.362 |
| **2** | **0.364** | **0.221** | 0.456 | 0.383 |
| 5 | 0.296 | 0.173 | 0.375 | 0.378 |
| 9 | 0.346 | 0.211 | **0.489** | 0.351 |

**해석 유의** — 1클래스 모델과 직접 비교하면 안 된다.
1클래스는 "사람을 찾았나", 2클래스는 "찾고 자세까지 맞췄나"로 **과제가 다르다.**

초반 하락은 이전 실행에서도 나타났다. `stage1_nomad` 는 ep2 0.551 → **ep3 0.310** 으로
폭락한 뒤 최종 0.606 을 냈다. 다만 pose2 는 9 epoch 째 ep2 기록을 못 넘고 있어
patience 8 로 조기 종료될 가능성이 있다.

**그렇게 끝나면 다음 수순** — best 가 ep2(warmup 중)가 되어 좋은 모델이 아니다.
헤드만 새로 배우는 상황이라 기본 학습률 0.01 이 높았을 가능성이 크므로,
**lr0 를 0.002 수준으로 낮춰 재학습**하는 것이 다음 시도다.

---

## 6. 합성 혼합 — 중단됨

| 항목 | 값 |
| --- | --- |
| 실행 | `runs_person/stage2_synth` — **results.csv 없음** |
| 계획 | 통합 모델 + 합성 411장 × 3배 (합성 비중 7.9 %) |
| 중단 사유 | 학습 중 게임 실행으로 시스템 RAM 부족, 크래시 |

**진행 전에 잡은 결함** — 첫 실행에서 합성 데이터가 **0장 섞였다.**
실사 크롭은 `.jpg` 인데 합성은 `.png` 였고 목록 생성이 `*.jpg` 만 찾고 있었다.
에러 없이 조용히 0장이었으므로, 그대로 돌렸다면 실사만으로 학습해 놓고
"합성은 효과 없음"이라고 잘못 결론 냈을 것이다. 확장자 고정을 제거하고
합성이 0장이면 즉시 중단하도록 방어 코드를 넣었다.

**재개 조건** — GPU·RAM 여유. 목적은 쓰러짐 재현율 보강이다.

---

## 7. 공통 설정과 근거

| 항목 | 값 | 근거 |
| --- | --- | --- |
| 아키텍처 | YOLOv8s (11.14 M) | yolov8n 대비 소형 객체 정확도. 추론 21.1 FPS 로 요구 5 FPS 대비 4배 여유 |
| 학습·추론 해상도 | 960 | 검증셋 사람 크기가 이미 충분(중앙 73 px, 20 px 미만 0 %)해 1280 으로 올릴 근거 없음 |
| batch | 8 (VRAM 4 GB) | batch 1 은 학습이 크게 흔들렸고, 자동 산정은 실패했다 |
| 색상 증강 | hsv_s 0.8 · hsv_v 0.5 | NOMAD 가 여름·정오 단일 조명이라 기본보다 강하게 |
| 데이터 분할 | 배우 단위(NOMAD) · 비행 단위(WiSARD) | 프레임 단위로 섞으면 연속 프레임 상관으로 성능이 부풀려진다 |
| 겨울 검증 확보 | 1월은 앞 80 % 학습 / 뒤 20 % 검증 | 비행이 하나뿐이라 시간 분할. 겨울 성능을 따로 말하기 위함 |

---

## 8. 알려진 한계

- **겨울 0.922 는 낙관적인 값이다.** 겨울 데이터가 비행 1회뿐이라 같은 장소·같은 조명이다.
  학습 프레임을 검증에 쓰지는 않았으나 다른 산에서는 더 낮다.
- **"9월 산지" 평가는 무효였다.** 검증용 비행 2개(289장)에 사람이 한 명도 없었다.
  세 모델 모두 0.000 이 나온 것은 성능이 아니라 측정 대상이 없었던 것이며,
  이 289장은 배경 오탐 측정으로 돌려 썼다.
- **쓰러짐 검증 표본이 209개**라 클래스별 수치의 신뢰구간이 ±6.6p 다. 작은 차이는 의미가 없다.
- **한국 데이터가 없다.** 학습 데이터는 미국(농장 · 워싱턴주 산악)이다.
- **실기체 검증이 없다.** 검증은 시뮬레이터 자율비행과 실제 항공촬영 데이터셋에서 했다.

---

## 9. 재현 방법

```bash
# 학습
python train_person.py --stage 1 --data <data.yaml> --name <이름> \
       --weights <시작가중치> --batch 8 --epochs 60

# 진행 확인 (30초마다 갱신)
python train_status.py --name <이름> --watch

# 도메인별 평가
python eval_domain.py

# 임계값별 성능
python tune_threshold.py --weights weights/<모델>.pt --out metrics/<이름>.csv
```

> 학습 중 `results.csv` 를 엑셀로 열지 말 것 — 파일 잠금으로 학습이 죽은 이력이 있다.

---

## 자세 분류 6차 시도 — Okutama 200px (2026-09-04, 실패)

**가설**: 지금까지 5번의 자세 분류 실패는 관측 해상도(97px) 때문이다.
Okutama-Action 4K 에서 사람 200px(실기체 고도 16m 수준)로 뽑으면 될 것이다.

| 항목 | 값 |
| --- | --- |
| 데이터 | Okutama 4K → 200px · train 2,068장(fallen 362) / val 746장(fallen 341) |
| 시작 가중치 | `yolov8s_stage1_all.pt` |
| 설정 | imgsz 960 · lr0 0.001 · 25에폭 (17에폭 조기 종료) |

**결과 — 클래스별 (best.pt, conf 0.15)**

| 클래스 | mAP50 | 정밀도 | 재현율 |
| --- | ---: | ---: | ---: |
| person | 0.621 | 0.525 | 0.755 |
| **fallen** | **0.058** | 0.413 | **0.111** |

**결론: 해상도 가설 기각.**
사람을 찾는 것(person 0.621)은 되는데 쓰러진 사람 판별(fallen 0.058)은
사실상 작동하지 않는다. 97px 로 했던 5차(0.341)와 200px 로 한 6차가
거의 같다 — 픽셀이 원인이 아니었다.

전체 mAP50 0.444 는 person 이 끌어올린 값이라 자세 판별 성능이 아니다.
클래스별로 나눠 보지 않으면 오독하기 쉽다.

**자세 분류 시도 전체 기록**

| 차수 | 방식 | 해상도 | 쓰러짐 성능 |
| --- | --- | ---: | ---: |
| 1 | 2클래스 탐지기 (lr 0.01) | 97px | 재현율 0.153 |
| 2 | 별도 분류기 | 97px | F1 0.297 |
| 3 | 합성 데이터 혼합 | 97px | F1 0.37~0.47 |
| 4 | 키포인트 (COCO 사전학습) | 97px | 재현율 0.2 |
| 5 | 2클래스 탐지기 (lr 0.001) | 97px | mAP50 0.341 |
| 6 | Okutama 2클래스 | **200px** | **mAP50 0.058** |

→ **자세 자동 판정은 포기한다.** SRS 에는 "탐지 + 스냅샷 제공" 으로 명세하고,
   쓰러졌는지 여부는 관제 화면에서 사람이 판단한다.
   사람을 *찾는* 것은 이미 검증됐다 (겨울 mAP50 0.922 · 쓰러진 사람 발견 11배).

---

## 자세 3클래스 — 8·9차 (2026-09-05)

person / fallen / **ambiguous** 3클래스로 바꾸고 백본을 동결했다.
동결은 파국적 망각을 막기 위한 것이다 — 7차(SARD 단독)에서 탐지 강건성이
NOMAD 0.667→0.290, WiSARD 0.884→0.754 로 무너졌다.

| 차수 | 실행 | 학습 데이터 | 설정 | SARD test 쓰러짐 |
|---|---|---|---|---|
| 8 | `pose3_sn_freeze` | SARD+NOMAD 9,008장 | 백본 10층 동결 · lr0 0.001 | 재현율 0.974 |
| 9 | `pose3_nd_freeze` | 위에서 SARD `not_defined` 제외 8,937장 | 동일 | **재현율 0.987** |

### 탐지 강건성 (클래스 무시 발견율)

| 모델 | NOMAD | WiSARD | Okutama(미학습) |
|---|---|---|---|
| `stage1_all` (원본) | **0.663** | **0.897** | 0.633 |
| `sard2_pose6` (7차) | 0.403 | 0.624 | 0.482 |
| `pose3_sn_freeze` (8차) | 0.589 | 0.804 | **0.682** |
| `pose3_nd_freeze` (9차) | 0.503 | 0.829 | 0.669 |

**백본 동결이 망각을 1/3로 줄였다** (−39%/−30% → −11%/−10%).
8차는 미학습 도메인(Okutama)에서 원본 모델을 앞선다.

### 9차 판정 — 채택하지 않음

`not_defined` 887개를 배경으로 돌리자 **실영상 `ambiguous` 오탐이 58% 줄었고**
(2,012→839건) 교차 도메인 정밀도가 전부 올랐다. 진단은 맞았다.

그러나 **NOMAD 발견율이 0.589→0.503 으로 −0.086** 떨어졌다.
재난 탐색에서 미탐지 비용이 오탐 비용보다 크므로 채택하지 않는다.
운용 모델은 **8차 `pose3_sn_freeze`** 를 쓴다.

### 남은 한계

| 항목 | 수치 |
|---|---|
| 도메인 이전 | Okutama 쓰러짐 0.091(8차) / 0.070(9차) |
| 탐지 손실 | 원본 대비 −10% 잔존 |
| `ambiguous` | 실영상에서 물체(벤치·가방)에 오탐 |

NOMAD·Okutama 의 쓰러짐이 0.15~0.25 에 갇히는 것은 **라벨 품질의 천장**으로 보인다.
그 도메인에 특화 학습한 모델조차 0.177 / 0.147 이었다.
SARD 만 0.96 을 넘는데, 유일하게 **사람이 직접 자세를 라벨링**한 데이터다.

### 속도

| 입력 | 추론 | 처리량 |
|---|---|---|
| 3840×2160 | 53.2 ms | 18.8 fps |
| **1280×720 (실기체)** | **20.6 ms** | **48.6 fps** |

RTX 3050 Laptop 4GB 기준. 온보드 장비에서는 재측정 필요.

### 재현

```
python make_pose3_dataset.py --wisard exclude            # 8차 데이터
python make_pose3_dataset.py --wisard exclude --sard-nd drop   # 9차 데이터
python train_person.py --stage 1 --data configs/data_pose3_sn.yaml   --weights weights/yolov8s_stage1_all.pt --name pose3_sn_freeze   --epochs 35 --imgsz 960 --batch 6 --lr0 0.001 --patience 10 --freeze 10
python eval_pose3.py --weights weights/yolov8s_pose3_sn_freeze.pt
```
