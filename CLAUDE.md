# CLAUDE.md — drone_yolo 작업 맥락

이 저장소에서 작업하는 Claude 세션이 **맨 먼저** 읽는 파일이다.
노트북 세션(정서인 · 2026-08 ~ 09)에서 확인한 사실·결정·함정을 모았다.
노트북 대화 기록은 서버에서 열 수 없으므로 **여기와 링크된 문서가 유일한 인계 경로다.**

갱신 2026-09-12 · 세부 수치는 링크 문서가 원본이고, 여기는 요약이다.

---

## 1. 과제와 운용 조건

「자율 정찰 드론 관제 시스템」 비전 파트 — 고령 인구 밀집 농촌에서 **쓰러진 사람을 찾아**
화면 좌표를 실좌표로 바꿔 관제 지도에 신고한다. 팀: 이시원(관제) · 서성훈(비행) · 정서인(비전).

| 항목 | 값 |
|---|---|
| 카메라 | IMX415 + 5.5 mm · HFOV 54.0° · **1920×1080** · 무짐벌 **하향 90°** |
| 고도 | 16 ~ 30 m · 폭 W = 2·고도·tan27° · GSD = W/1920 |
| 16 m | 서 있는 사람(위에서 본 어깨폭) **59 px** · 누운 사람 **200 px** |
| 30 m | 서 있는 사람 **31 px** · 누운 사람 **107 px** |
| imgsz 960 입력 | 위 수치가 절반 → 30 m 서 있는 사람 **15.5 px** |
| 분석 입력 (SRS NFR-P06) | 1280×720 (1080p 수신 후 축소) |

**모든 판단은 이 표 기준이다.** 하향 90°라 영상에 "위쪽"이 없다 → 회전 180° · 상하반전 증강이 맞다.

### 요구명세서 v1.1 의 비전 목표 (합격선)

| ID | 기준 |
|---|---|
| NFR-V01 | 1280×720 추론 평균 **≤30 ms** |
| NFR-V03 | 겨울 mAP50 **≥0.85** · 여름 **≥0.60** · *학습/평가 장소 분리* |
| NFR-V05 | conf 기본 **0.15** (F2 로 선정) |
| NFR-V06 | 사람 없는 영상에서 오탐 **≤1건/프레임** |
| NFR-V07(제안) | 쓰러짐 재현율 **≥0.70** · 부분 가림 ≥0.50 |

---

## 2. 트랙 현황 (2026-09-12)

| 트랙 | 어디서 | 내용 | 상태 |
|---|---|---|---|
| **탐지 스윕 E1~E8** | 서버 `~/JupyterLAB/drone_dev` · 브랜치 `server-3090-setup` | COCO 사전학습(yolo11s/v8s/11m/11l)에서 새로 · 1클래스 · NOMAD 42명 + WiSARD VIS · degrees 180 / flipud 0.5 | **진행 중** (`EXPERIMENTS.md`) |
| 3클래스 자세 pose3 | 노트북 | person / fallen / ambiguous · `stage1_all` 에서 백본 동결 전이학습 | 8차 운용안 확보 · 서버 재개는 E 트랙 이후 (`REBOOT_PLAN.md`) |
| Copy-Paste 증강 | 노트북 | SAM 인스턴스 은행 준비 완료 | **보류** |
| 좌표 환산 | 노트북 | 시뮬레이터 깊이 기반 평균 0.50 m | 실기체엔 하향 깊이 없음 → 평지/DEM 방식으로 교체 필요 |

---

## 3. 확정된 수치

### 탐지 — `stage1_all` (VisDrone → NOMAD 1~30 + WiSARD, 1클래스)

| 도메인 | mAP50 | 재현율 |
|---|---:|---:|
| NOMAD 여름 | 0.641 | 0.606 |
| WiSARD 1월 겨울 | 0.922 ⚠ | 0.824 |
| 통합 2,769장 | 0.850 | 0.767 |
| 쓰러진 사람 (conf 0.15) | — | 0.608 |
| 배경 오탐 | 0.99건/장 | |

⚠ 겨울 0.922 는 **같은 비행을 시간순으로 자른 뒤쪽**으로 잰 값이라 NFR-V03 장소 분리를 만족하지 않는다.

### 자세 — `eval_pose3.py` 기준 (conf 0.15 · IoU 0.3)

| 모델 | NOMAD 발견 | WiSARD 발견 | Okutama(미학습) 발견 | SARD test 쓰러짐 | NOMAD 쓰러짐 | Okutama 쓰러짐 |
|---|---:|---:|---:|---:|---:|---:|
| stage1_all (1클래스) | **0.663** | **0.897** | 0.633 | — | — | — |
| 7차 sard2_pose6 (동결 없음) | 0.403 | 0.624 | 0.482 | 0.960 | 0.157 | 0.088 |
| **8차 pose3_sn_freeze** ★ | 0.589 | 0.804 | **0.682** | 0.974 | 0.227 | 0.091 |
| 9차 pose3_nd_freeze (기각) | 0.503 | 0.829 | 0.669 | 0.987 | 0.254 | 0.070 |

- 동결(`--freeze 10`)이 탐지 망각을 −39% → −11% 로 줄였다.
- **쓰러짐 판별은 SARD 밖으로 나가면 무너진다** (0.974 → Okutama 0.091). 이게 가장 큰 미해결 문제다.
- 실영상 2.2.10 에서 `ambiguous` 가 전체 탐지의 47% — 벤치·가방에 붙는다.
- 속도: 1280×720 **20.6 ms · 48.6 fps** (RTX 3050 노트북, P95 21.8 ms)

---

## 4. ⚠ 서버 E 트랙에 바로 해당하는 것

1. **NOMAD 검증셋이 노트북과 다르다 → 노트북 수치와 직접 비교 불가.** (2026-09-12 양쪽 확인)
   `nomad_prep.py` 는 실행(배치)마다 seed 42 로 그 배치 배우만 섞어 20% 를 val 로 뽑는다.

   | 배치 | 노트북 (`stage1_all`) | 서버 (E1~E8) |
   |---|---|---|
   | 001~010 | val 004 · 008 | val 004 · 008 |
   | 011~020 | val 014 · 018 | val 014 · 018 |
   | 021~030 | **`--all-train` → val 없음** (024 · 028 도 학습) | val **024 · 028** |
   | 선정 목록 31번 이후 30명 | 사용 안 함 | val 048 · 059 · 071 · 079 · 088 · 094 |

   - E1~E8 **서로 간** 비교는 문제없다.
   - `EXPERIMENTS.md` 기준선(여름 0.641, val 4명)과는 검증셋이 달라 비교하면 안 된다.
   - `stage1_all` 을 서버 val 로 재면 **024 · 028 이 그 모델의 학습 배우라 부풀려진다.**
   - **공정한 공통 홀드아웃 = 서버 val 에서 024 · 028 을 뺀 10명**
     `004 · 008 · 014 · 018 · 048 · 059 · 071 · 079 · 088 · 094`. 최종 승자와 `stage1_all` 은 이것으로 비교한다.
2. **조기 종료는 mAP50 이 아니라 fitness = 0.1·mAP50 + 0.9·mAP50-95** 로 판단된다.
3. **초반 2~4 에폭 하락은 warmup 때문이다.** 그걸로 끊지 말 것.
4. **WiSARD val 앞쪽 291장은 사람이 없는 음성 표본이다.** `--limit` 으로 앞에서부터 자르면 재현율이 비어 나온다.
5. **소표본으로 결론 내지 말 것.** 70장 진단에서 "WiSARD 0.887→0.883 유지"라 했다가 전체로 재니 0.804 였다.
6. **imgsz 는 1280 까지만.** NOMAD 크롭 원본이 1280×720 이다. NOMAD 크롭은 사람 박스를 약 94 px 에 맞춰
   만들었으므로, 30 m 운용(서 있는 사람 31 px)보다 사람이 크다 — 고도별 평가는 `metrics/eval_altitude.csv` 참고.
7. **Okutama 는 평가 전용이다** (배우 수가 적다). 서버에 없으면 그 도메인만 빠진다.
8. 가중치 출처가 궁금하면 `WEIGHTS.md`. 노트북 가중치는 **`--resume` 금지**(Windows 경로가 박혀 있음) — 쓰려면 `--weights`.

---

## 5. 결정과 이유 (다시 논의하지 않아도 되는 것)

| 결정 | 이유 |
|---|---|
| 자세는 3클래스 person / fallen / ambiguous | SARD `not_defined` 와 NOMAD 전환 구간을 억지로 둘 중 하나에 넣지 않기 위해 |
| NOMAD 활동 경계 **±15프레임** → ambiguous | 눕는 중·일어나는 중. fallen 2,703 → 1,844 (68.2% 유지). 30프레임은 52.4% 로 과함 |
| **WiSARD 는 자세 학습에서 제외** | WiSARD `person` 은 "사람 있음"(탐지) 라벨, 우리 `person` 은 "서 있음"(자세). 뜻이 다르다. 겨울 강건성은 백본 동결로 지킨다 |
| SARD `not_defined` 유지 (9차 기각) | 빼면 물체 오탐은 58% 줄지만 NOMAD 발견율 0.589 → 0.503 |
| Okutama 학습 제외 | 영상 40편·배우 소수 → 다양성 부족. 평가 도메인으로만 |
| Copy-Paste 보류 | 은행은 준비됨(SARD 376 · Okutama 149 · NOMAD fallen 591 · person 482). 9월 농장 배경 우선. 팀 합의 후 재개 |

---

## 6. 막다른 길 — 다시 하지 말 것

- **마스크 기하(박스 종횡비·PCA 축비)로 fallen/person 구분** — AUC 0.342 / 0.387, 오히려 반대 방향
- **200 px 로 키워 자세 분류** (Okutama 6차) — 해상도 가설 기각
- **학습률만 낮추기** — 동결 없이 lr 만 낮춰서는 망각을 못 막는다
- **CloudTrack(arXiv 2409.16111) 수치와 mAP 비교** — 제로샷 VLM(SARD pose_laying 42.77%)이라 지도학습과 비교 대상이 아니다. 비교할 수 있는 건 속도(6.955 s/frame)뿐
- **AI-Hub 한국 데이터** — 촬영각 45° 라 하향 90° 조건과 맞지 않는다
- **ForestPersons(ETRI)** — 지상 1.5~2 m 시점

---

## 7. 지켜야 할 규칙

- **NOMAD · WiSARD · Okutama 원본과 파생물(크롭·인스턴스 은행)은 git 에 올리지 않는다.** 연구용 라이선스다
- rclone OAuth 토큰 · Roboflow/Kaggle 키는 문서·채팅·커밋에 남기지 않는다
- 파일 삭제 · 전역 설정 변경 · 패키지 설치 전에는 무엇을 왜 하는지 사용자에게 먼저 설명한다
- 3클래스 자세 트랙을 서버에서 재개할 때는 `ultralytics==8.4.102` 고정 (노트북 기준선과 비교하려면)
- 학습이 끝날 때마다 `results.csv` 와 `best.pt` 를 바로 백업한다 (대여 서버)

---

## 8. 읽는 순서

| 순서 | 문서 | 무엇 |
|---|---|---|
| 1 | 이 파일 | 전체 맥락 |
| 2 | `EXPERIMENTS.md` | 서버 E1~E8 계획 |
| 3 | `SERVER.md` | 3090 설정 · 실험 A~E · 함정 7가지 |
| 4 | `WEIGHTS.md` | 노트북 가중치 계보와 신뢰 범위 |
| 5 | `DATASETS.md` | 데이터 받는 법 (rclone · gdown · Roboflow) |
| 6 | `MODELS.md` | 1~9차 전체 기록 |
| 7 | `REBOOT_PLAN.md` | 자세 트랙 서버 재개 절차 |
| 참고 | `tim9985/Capstone` → `보고서/비전_문서/06·07` | 자세 라벨 설계 논의 · 3클래스 실험 기록 (팀 공유용 HTML) |

### 도구

| 스크립트 | 용도 |
|---|---|
| `nomad_prep.py` · `wisard_prep.py` · `make_configs.py` | 원본 → YOLO 데이터셋 · yaml 절대경로 재생성 |
| `make_pose3_dataset.py` | 3클래스 자세 데이터셋 (`--wisard exclude --trim-frames 15`) |
| `train_person.py` | 학습 (`--weights` 미지정 시 VisDrone 가중치에서 시작) |
| `eval_pose3.py` | 도메인별 발견율 · 쓰러짐 재현율/정밀도 → `metrics/eval_pose3.csv` |
| `video_infer.py` | 영상 일괄 추론 → mp4 · CSV · 쓰러짐 스냅샷 (서버에서 가능) |
| `live_view.py` | 실시간 창 표시 — **화면이 필요해 서버에선 못 쓴다** |
| `check_env.sh` | 환경 점검 |

---

## 9. 노트북 ↔ 서버 동기화

- 서버 진행 상황은 **`SERVER_PROGRESS.md` 에 쓰고 브랜치를 push** 해야 노트북에서 보인다.
  push 는 사용자 토큰이 필요하다.
- 이 파일은 `main` 에 있다. 브랜치에서 받으려면 `git fetch origin && git merge origin/main`
  (또는 `git checkout origin/main -- CLAUDE.md`).
- 노트북에만 있는 것: Okutama 원본(9.6 GB) · 인스턴스 은행 · 실영상 추론 결과(`runs_video/`).
